repositories {
    jcenter()
    flatDir {
        dirs 'aar'
    }
}

dependencies {
    compile(name:'mm-ad-sdk', ext:'aar')
    compile(name:'android-admob-mm-adapter', ext:'aar')
}

android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}